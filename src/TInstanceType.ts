export type TInstanceType = 'per-container' | 'per-dependency' | 'single';

export const INSTANCE_SINGLE: TInstanceType = 'single';
export const INSTANCE_PER_DEPENDENCY: TInstanceType = 'per-dependency';
export const INSTANCE_PER_CONTAINER: TInstanceType = 'per-container';
